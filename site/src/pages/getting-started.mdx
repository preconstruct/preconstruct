# Getting Started with preconstruct

Firstly, we need to install preconstruct

```bash
yarn add --dev preconstruct
```

We can initialise preconstruct with `preconstruct init`.

```bash
yarn preconstruct init
```

## What have we got now?

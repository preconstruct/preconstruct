# Configuring Babel

preconstruct uses Babel's normal configuration resolving logic so there isn't any preconstruct-specific way of configuring Babel but there are some things you'll want to keep in mind.

...an example configuring Babel

## Babel config in a monorepo

In a monorepo, you will mostly likely want to use a `babel.config.js` file over a `.babelrc`. This is because `.babelrc` files are only resolved up to the nearest package directory whereas in a monorepo, you will likely want

...an example configuring Babel with a `babel.config.js` file

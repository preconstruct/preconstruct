---
"@preconstruct/cli": major
---

Build a single CommonJS bundle and preserve `process.env.NODE_ENV`, `typeof window`, and `typeof document` expressions. Remove the legacy `preconstruct.exports.envConditions` option and its generated browser and worker condition bundles.
